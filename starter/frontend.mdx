---
title: "Starter · Frontend"
description: "React + Vite app configuration and capabilities."
---

## Environment

Set `apps/web/.env`:

```bash
VITE_API_URL=http://localhost:8080/api
VITE_WS_URL=http://localhost:8080
VITE_SOLANA_NETWORK=mainnet-beta
VITE_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
```

## Features

- Wallet connection (Phantom, Solflare, Backpack)
- Live trial updates via WebSocket
- Stats dashboards and settings pages
- Builder UI stubs

## Scripts

```bash
npm run dev -w @fare-casino/web
npm run build -w @fare-casino/web
npm run preview -w @fare-casino/web
```

